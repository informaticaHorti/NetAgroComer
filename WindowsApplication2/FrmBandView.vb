Imports DevExpress.XtraGrid.Views.BandedGrid

Public Class FrmBandView

    Public Sub New()

        ' Llamada necesaria para el diseñador.
        InitializeComponent()

        ' Agregue cualquier inicialización después de la llamada a InitializeComponent().

    End Sub

    Private Sub FrmBandView_Load(sender As Object, e As System.EventArgs) Handles Me.Load


        'Dim sql As String = "SELECT * FROM ZONAS"
        'Dim dt As DataTable = cn.ConsultaSQL(sql)

        'GridControl1.DataSource = dt

        'zona.OwnerBand = GridBand2
        'GridBand2.Columns.Add(zona)

       

    End Sub
End Class